---
// src/components/GsapAnimate.astro
---
<script type="module">
  import { animateHeroAndAbout } from '/scripts/gsap-about.js';

  // Llamamos a la animación cuando el script se ejecuta (solo en el cliente)
  window.addEventListener('DOMContentLoaded', () => {
    animateHeroAndAbout();
  });
</script>
